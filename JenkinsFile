pipeline {
    agent any

    tools{
        git "C:/Program Files/Git/cmd/git.exe"
    }

    stages {
        stage('Pull Repo') {
            steps {
                git pull origin main
            }
        }
    }
}